<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permiso para conexion a red -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permiso para usar almacenamiento externo del telefono -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GaticketApp"
        tools:targetApi="31">
        <activity
            android:name=".view.AdminDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.AdminListActivity"
            android:exported="false" />
        <activity
            android:name=".view.UserListActivity"
            android:exported="false" />
        <activity
            android:name=".view.UserMainActivity"
            android:exported="false" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>